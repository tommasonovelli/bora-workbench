{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qwen-launcher.dev/schemas/calibration-policy.v1.json",
  "title": "qwen-launcher calibration-policy/v1",
  "description": "Approved candidate and hardware-class policy for calibration/v1.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "benchmark_protocol",
    "gpu_poll_interval_ms",
    "gpu_release_stabilization_ms",
    "policies"
  ],
  "properties": {
    "schema": {
      "description": "Versioned schema identifier for this calibration policy catalog.",
      "const": "calibration-policy/v1"
    },
    "benchmark_protocol": {
      "description": "Benchmark protocol run for every valid calibration candidate.",
      "const": "benchmark/v1"
    },
    "gpu_poll_interval_ms": {
      "description": "Fixed interval in milliseconds for calibration/v1 GPU memory sampling.",
      "const": 250
    },
    "gpu_release_stabilization_ms": {
      "description": "Maximum post-stop stabilization window for calibration/v1 GPU memory release.",
      "const": 10000
    },
    "policies": {
      "description": "Non-empty collection of approved policies with semantically unique identifiers.",
      "type": "array",
      "minItems": 1,
      "items": {
        "description": "Policy for one model, engine, backend, and set of operating systems.",
        "$ref": "#/$defs/policy"
      }
    }
  },
  "$defs": {
    "range": {
      "description": "Inclusive GiB range where null means no upper bound.",
      "type": "array",
      "prefixItems": [
        {
          "description": "Inclusive minimum capacity in GiB.",
          "type": "number",
          "minimum": 0
        },
        {
          "description": "Inclusive maximum capacity in GiB, or null for no upper bound.",
          "anyOf": [
            {
              "type": "number",
              "minimum": 0
            },
            {
              "type": "null"
            }
          ]
        }
      ],
      "items": false,
      "minItems": 2,
      "maxItems": 2
    },
    "candidate": {
      "description": "One ordered performance-envelope candidate tested by calibration/v1.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "ctx"
      ],
      "properties": {
        "id": {
          "description": "Identifier unique within the containing mode candidate list.",
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "ctx": {
          "description": "Context size tested by this candidate.",
          "type": "integer",
          "minimum": 1024
        },
        "n_cpu_moe": {
          "description": "Experts assigned to CPU on CUDA; required on CUDA and forbidden on CPU by semantic validation.",
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "mode_policy": {
      "description": "Ordered candidates approved for one launcher mode.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "candidates"
      ],
      "properties": {
        "candidates": {
          "description": "Non-empty candidate list in the exact approved test order.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "Candidate to test for this mode.",
            "$ref": "#/$defs/candidate"
          }
        }
      }
    },
    "hardware_class": {
      "description": "Approved non-overlapping nominal RAM and VRAM class.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "ram_gib",
        "vram_gib"
      ],
      "properties": {
        "id": {
          "description": "Stable hardware-class identifier unique within its policy.",
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "ram_gib": {
          "description": "Inclusive nominal system RAM range in GiB.",
          "$ref": "#/$defs/range"
        },
        "vram_gib": {
          "description": "Inclusive nominal VRAM range in GiB.",
          "$ref": "#/$defs/range"
        }
      }
    },
    "policy": {
      "description": "Approved calibration policy for one execution target.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "model",
        "engine",
        "backend",
        "os",
        "stable_start_runs",
        "modes",
        "hardware_classes"
      ],
      "properties": {
        "id": {
          "description": "Stable policy identifier unique within this catalog.",
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "model": {
          "description": "Complete model identifier covered by this policy.",
          "type": "string",
          "minLength": 1
        },
        "engine": {
          "description": "Exact llama.cpp release covered by this policy.",
          "type": "string",
          "minLength": 1
        },
        "backend": {
          "description": "Execution backend covered by this policy.",
          "enum": [
            "cuda",
            "cpu"
          ]
        },
        "os": {
          "description": "Operating systems on which the policy was approved.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "description": "Supported operating system identifier.",
            "enum": [
              "linux",
              "windows"
            ]
          }
        },
        "stable_start_runs": {
          "description": "Number of successful fresh starts required for each valid candidate.",
          "type": "integer",
          "minimum": 1
        },
        "minimum_calibration_vram_gib": {
          "description": "Minimum total VRAM required to start CUDA calibration automatically.",
          "type": "number",
          "minimum": 0
        },
        "minimum_free_vram_gib": {
          "description": "Minimum free VRAM reserve required throughout a CUDA candidate run.",
          "type": "number",
          "minimum": 0
        },
        "vram_release_tolerance_gib": {
          "description": "Maximum aggregate used-VRAM increase accepted after the stabilization window.",
          "type": "number",
          "minimum": 0
        },
        "modes": {
          "description": "Approved calibration candidates keyed by mode identifier.",
          "type": "object",
          "minProperties": 1,
          "patternProperties": {
            "^[a-z0-9-]+$": {
              "description": "Calibration policy for one existing mode.",
              "$ref": "#/$defs/mode_policy"
            }
          },
          "additionalProperties": false
        },
        "hardware_classes": {
          "description": "Approved nominal hardware classes for this policy.",
          "type": "array",
          "minItems": 1,
          "items": {
            "description": "Hardware class recognized by this policy.",
            "$ref": "#/$defs/hardware_class"
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "backend": {
                "description": "Constraint selecting the CUDA-specific policy branch.",
                "const": "cuda"
              }
            },
            "required": [
              "backend"
            ]
          },
          "then": {
            "required": [
              "minimum_calibration_vram_gib",
              "minimum_free_vram_gib",
              "vram_release_tolerance_gib"
            ]
          },
          "else": {
            "not": {
              "anyOf": [
                {
                  "required": [
                    "minimum_calibration_vram_gib"
                  ]
                },
                {
                  "required": [
                    "minimum_free_vram_gib"
                  ]
                },
                {
                  "required": [
                    "vram_release_tolerance_gib"
                  ]
                }
              ]
            }
          }
        }
      ]
    }
  }
}
