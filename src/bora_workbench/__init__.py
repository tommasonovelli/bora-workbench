"""bora-workbench package.

Importing this package intentionally has no filesystem, network, or process side effects: a plain
`import bora_workbench` must never touch the user's machine (specification sections 4.3 and 5.11).
This module therefore stays empty of imports and executable statements on purpose.
"""
